---
all:
  children:
    truenas:
      hosts:
        localhost:
          ansible_connection: local
    cachyos:
      hosts:
        192.168.1.191:
          ansible_user: wyatt
          ansible_become: true
