all:
  children:
    truenas:
      hosts:
        truenas-primary:
          ansible_host: 192.168.1.3
          ansible_user: root
          ansible_connection: ssh
          ansible_ssh_common_args: -o StrictHostKeyChecking=no
