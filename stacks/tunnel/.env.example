# Cloudflare Tunnel Configuration
# Setup: https://one.dash.cloudflare.com/ → Networks → Tunnels → Create
# 1. Create tunnel, choose "Cloudflared" connector
# 2. Add public hostname: deploy.wyattau.com
# 3. Service: http://localhost:9001
# 4. Copy the tunnel token here

TUNNEL_TOKEN=GENERATE_THIS
