# Calibre-Web image uses :latest directly (linuxserver/calibre-web).
# No version pinning — the linuxserver image is updated infrequently
# and the embedded Calibre version is pinned via DOCKER_MODS env var.
