log:
  level: debug

runner:
  file: /mnt/pool_HDD_x2/infra/act-runner/data/general-k8s/.runner
  capacity: 1
  timeout: 2h
  insecure: true
  fetch_timeout: 30s
  labels:
    - "general"
    - "docker"

kubernetes:
  namespace: ci-jobs
  kubeconfig: /etc/rancher/k3s/k3s.yaml
  poller:
    enabled: true
