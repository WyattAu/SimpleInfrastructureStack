apiVersion: 1
contactPoints:
  - orgId: 1
    name: ntfy
    receivers:
      - uid: ntfy-push
        type: webhook
        settings:
          url: https://ntfy.sh
          # Configure after first deploy via Grafana UI:
          # Alerting → Contact Points → ntfy → Edit
          # Set URL to: https://ntfy.sh/<your-topic>?auth=<your-ntfy-token>
