# Terraform state files (contain resource IDs, may contain some sensitive data)
*.tfstate
*.tfstate.backup

# Terraform variables with secrets (set via TF_VAR_ env vars instead)
*.tfvars
*.tfvars.json
*.auto.tfvars

# Provider binaries
.terraform/

# Plan files (binary, environment-specific)
*.tfplan
tfplan*

